<div id="<%= dom_id user %>">
  <% content_for(:title, @user.name) %>
  <h1>
    <%= gravatar_for @user, size: 100 %>
    <%= @user.name %>
  </h1>

  <p>
    <strong>Name:</strong>
    <%= user.name %>
  </p>

  <p>
    <strong>Email:</strong>
    <%= user.email %>
  </p>

</div>
